{
  "$schema": "https://modelcontextprotocol.io/schemas/mcp.schema.json",
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "Official GitHub MCP server (remote, hosted by GitHub). Provides tools for repositories, issues, pull requests, actions, code search, and more."
    },
    "github-local": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "Official GitHub MCP server running locally via Docker. Use this when remote MCP is unavailable or restricted by network policy.",
      "disabled": true
    }
  }
}
